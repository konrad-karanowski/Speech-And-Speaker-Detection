# metadata specialised for each experiment
core:
  project_name: 'Speech_And_Speaker_Detection'
  storage_dir: ${oc.env:PROJECT_ROOT}/logs
  version: 0.0.1
  tags: null

defaults:
  - hydra: default
  - model: pre_train
  - datamodule: pre_train
  - train: pre_train
  - _self_
  # - _self_ 
#  - hydra/launcher: joblib